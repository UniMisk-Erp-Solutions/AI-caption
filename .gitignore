node_modules/
dist/
.wrangler/
*.log
.DS_Store

# Secrets. Never commit these - they hold live API keys.
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
!.dev.vars.example
*.pem
*.key

# Benchmark inputs and outputs. The clips are the user's own media and the
# results are a point-in-time measurement, not source.
test-*.mp4
bench-results.json
